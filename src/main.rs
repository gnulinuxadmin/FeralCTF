// FeralCTF - Main entry point
// See Sprint 0 requirements in FERALCTF_SPRINTS.md
// Logic will be added in Sprint 5+ (server startup, signal handling, CLI subcommands)

#[tokio::main]
async fn main() -> Result<(), Box<dyn std::error::Error>> {
    Ok(())
}
